apiVersion: apps/v1
kind: Deployment
metadata:
  name: pst-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pst-web
  template:
    metadata:
      labels:
        app: pst-web
    spec:
      containers:
        - name: web
          image: europe-west1-docker.pkg.dev/project-c08bd28e-4c9b-4e57-9da/toolshop/web-k8s:35bce3f
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: API_WEB_HOST
              value: apiweb          # ← was pst-apiweb
            - name: API_WEB_PORT
              value: "80"
# Load Balancer needed if app needs to be exposed to internet.
# External ip is assigned in this case that can be used.
# Wi will be using port-forward for now (so we need cluster IP instead)
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: pst-web
# spec:
#   type: LoadBalancer
#   selector:
#     app: pst-web
#   ports:
#     - name: http
#       port: 80
#       targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: pst-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: pst-web